let x, y; //se pueden crear variables en una misma fila

x = 20, y = 30;//se lle puede asigar los valores en una misma linea

let z = x + y;//se asigna el valor de la operacion
console.log(z);

let _nume1 = 4;//no se utiliza numeros en la primea posicion para asignarle el nombre ala variables
let /*break*/ rompiendo = "break" //no se pueden asignar palabras reservadas para nombrar variables
